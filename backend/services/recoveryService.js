const crypto = require('crypto'); const bcrypt = require('bcrypt'); const Recovery = require('../models/Recovery'); const User = require('../models/User'); const { ApiError } = require('../utils/helpers'); const verificationService = require('./verificationService'); const turnkeyService = require('./turnkeyService');
const OTP_TTL_MS = 10 * 60 * 1000; const RECOVERY_TTL_MS = 60 * 60 * 1000;
async function startRecovery({ email }) { const user = await User.findOne({ email }); if (!user) throw new ApiError(404, 'No account matches that email address.'); const otp = crypto.randomInt(100000, 1000000).toString(); const recovery = await Recovery.create({ userId: user._id, email, otpHash: await bcrypt.hash(otp, Number(process.env.BCRYPT_ROUNDS) || 12), otpExpiresAt: new Date(Date.now() + OTP_TTL_MS), expiresAt: new Date(Date.now() + RECOVERY_TTL_MS) }); await verificationService.startRecovery({ recoveryId: recovery._id.toString(), userId: user._id.toString() }); return { recoveryId: recovery._id, expiresAt: recovery.expiresAt, ...(process.env.NODE_ENV === 'development' ? { developmentOtp: otp } : {}) }; }
async function verifyEmail({ recoveryId, otp }) { const recovery = await Recovery.findById(recoveryId).select('+otpHash'); if (!recovery || recovery.expiresAt < new Date()) throw new ApiError(400, 'Recovery request has expired.'); if (recovery.attempts >= 5) throw new ApiError(429, 'Too many verification attempts.'); recovery.attempts += 1; if (!recovery.otpExpiresAt || !(await bcrypt.compare(otp, recovery.otpHash))) { await recovery.save(); throw new ApiError(400, 'Invalid or expired one-time code.'); } recovery.emailVerified = true; recovery.recoveryStatus = 'email_verified'; recovery.otpHash = undefined; await recovery.save(); return recovery; }
async function verifyVisualPassword({ recoveryId, verificationPayload }) { const recovery = await Recovery.findById(recoveryId); if (!recovery || recovery.expiresAt < new Date()) throw new ApiError(400, 'Recovery request has expired.'); if (!recovery.emailVerified) throw new ApiError(409, 'Verify email before the Visual Password step.'); const result = await verificationService.verifyRecovery({ recoveryId, ...verificationPayload }); if (!result?.verified) throw new ApiError(403, 'Visual Password verification was not accepted.'); recovery.visualPasswordVerified = true; recovery.recoveryStatus = 'visual_password_verified'; await recovery.save(); return recovery; }
async function createPasskey({ recoveryId, passkeyAttestation }) { const recovery = await Recovery.findById(recoveryId).populate('userId'); if (!recovery || !recovery.visualPasswordVerified) throw new ApiError(409, 'Complete recovery verification before creating a passkey.'); await turnkeyService.registerRecoveryPasskey({ turnkeyUserId: recovery.userId.turnkeyUserId, passkeyAttestation }); recovery.recoveryStatus = 'completed'; await recovery.save(); return recovery; }
module.exports = { startRecovery, verifyEmail, verifyVisualPassword, createPasskey };
